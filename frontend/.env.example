# In development this is left empty — Vite's server.proxy forwards /predict to Flask.
# In production set this to your deployed API URL (no trailing slash):
# VITE_API_BASE_URL=https://your-api.fly.dev
VITE_API_BASE_URL=
