#
# Production runtime dependencies for the Flask API in `backend/app.py`.
#
# Notes:
# - Avoid heavy/unused deps (e.g. catboost, matplotlib, plotly) to keep deploy
#   bundles small on serverless platforms.
# Pin scikit-learn to the version used when saving model/transformer.pkl
# (joblib pickles are not portable across sklearn minor versions).
# If you re-export pickles from a newer sklearn, bump this pin to match.
#

# Web server / API
Flask==3.0.0
gunicorn==20.1.0
Werkzeug==3.0.1
Flask-Cors==4.0.0
python-dotenv==0.21.1

# Auth (keep if/when you add auth routes)
Flask-JWT-Extended==4.6.0

# ML inference runtime (must match what your pickles need)
numpy
pandas
scikit-learn==1.6.1
joblib

# DB libs are optional right now (you currently init SQLAlchemy but don't use it
# for /predict). If you don't need DB in production, consider removing these
# and the SQLAlchemy setup in app.py.
Flask-SQLAlchemy==3.0.1
SQLAlchemy>=1.4,<2.0
